pipeline {
    agent { dockerfile true }
    stages {
        stage('Test Env') {
            steps {
               echo 'Hello world"
              sh 'echo name = $name'   
         
            }
        }
    }
}
